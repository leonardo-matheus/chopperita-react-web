import React from "react";

const Preloader: React.FC = () => (
  <div id="preloader">
    <div id="loader">
      {/* Adicione qualquer conteúdo adicional que você deseja exibir no preloader */}
    </div>
  </div>
);

export default Preloader;
